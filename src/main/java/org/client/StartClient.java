package org.client;

/**
 * The StartClient class contains the main method to start the Go game client.
 * It instantiates a new Client object to initiate the client-side application.
 */
public class StartClient {
    /**
     * The main method that starts the Go game client.
     *
     * @param args Command-line arguments (not used in this application).
     */
    public static void main(String[] args) {
        new Client();
    }
}
